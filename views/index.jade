<!doctype html>
html(ng-app="")
    head
        title workroom

        != css("app")

        if queue
            script.
                old_queue = !{queue}

        script(src="/socket.io/socket.io.js")

        != js("app")

    body.center
        div.header
            a(href="")
                h1 workroom
        div.editor-container
            textarea#editor= content
            div.resize
        iframe#view
        textarea.hide

